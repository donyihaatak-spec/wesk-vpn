//! Доменная логика VPN: модель/парсер конфигураций и их хранилище.
//!
//! Модуль намеренно не зависит от Tauri (кроме инициализации путей в
//! `store`), чтобы бизнес-логику можно было покрывать юнит-тестами и
//! переиспользовать. IPC-обёртки находятся в `crate::commands`.

pub mod config;
pub mod manager;
pub mod store;
